% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagsep.R, R/generics.R, R/methods.R
\name{diagsep}
\alias{diagsep}
\alias{diagnose_separation}
\alias{diagnose_separation.default}
\alias{diagnose_separation.factor}
\alias{diagnose_separation.matrix}
\alias{diagnose_separation.formula}
\alias{diagnose_separation.osm}
\alias{diagnose_separation.clm}
\alias{diagnose_separation.polr}
\alias{diagnose_separation.multinom}
\alias{diagnose_separation.glm}
\alias{diagnose_separation.bracl}
\alias{diagnose_separation.brmultinom}
\title{Detailed separation diagnostic for all categorical outcomes.}
\usage{
diagsep(
  y,
  X,
  S,
  rational = FALSE,
  model = c("bcl", "b", "cl", "acl", "sl", "osm"),
  backend = c("rcdd", "ROI"),
  solver = NULL
)

diagnose_separation(..., rational, backend, solver)

\method{diagnose_separation}{default}(
  y,
  X,
  rational = FALSE,
  backend = c("rcdd", "ROI"),
  solver = NULL,
  ...
)

\method{diagnose_separation}{factor}(
  y,
  X,
  rational = FALSE,
  backend = c("rcdd", "ROI"),
  solver = NULL,
  ...
)

\method{diagnose_separation}{matrix}(
  S,
  rational = FALSE,
  backend = c("rcdd", "ROI"),
  solver = NULL,
  ...
)

\method{diagnose_separation}{formula}(
  formula,
  data,
  model = c("bcl", "b", "cl", "acl", "osm", "sl"),
  rational = FALSE,
  contrasts = NULL,
  backend = c("rcdd", "ROI"),
  solver = NULL,
  ...
)

\method{diagnose_separation}{osm}(
  object,
  rational = FALSE,
  backend = c("rcdd", "ROI"),
  solver = NULL,
  ...
)

\method{diagnose_separation}{clm}(
  object,
  rational = FALSE,
  backend = c("rcdd", "ROI"),
  solver = NULL,
  ...
)

\method{diagnose_separation}{polr}(
  object,
  rational = FALSE,
  backend = c("rcdd", "ROI"),
  solver = NULL,
  ...
)

\method{diagnose_separation}{multinom}(
  object,
  rational = FALSE,
  backend = c("rcdd", "ROI"),
  solver = NULL,
  ...
)

\method{diagnose_separation}{glm}(
  object,
  rational = FALSE,
  backend = c("rcdd", "ROI"),
  solver = NULL,
  ...
)

\method{diagnose_separation}{bracl}(
  object,
  rational = FALSE,
  backend = c("rcdd", "ROI"),
  solver = NULL,
  ...
)

\method{diagnose_separation}{brmultinom}(
  object,
  rational = FALSE,
  backend = c("rcdd", "ROI"),
  solver = NULL,
  ...
)
}
\arguments{
\item{y}{the outcome variable. Can be binary, categorial or ordinal. Works best if it is an ordered or unordered factor but can also be numeric, boolean or character. If y is not a factor, it is treated as a nominal (categorical) outcome.}

\item{X}{a design matrix, e.g. generated via a call to 'model.matrix'. This means we expect that X already contains the desired contrasts for factors (e.g., dummies) and any other expanded columns (e.g., for polynomials).}

\item{S}{a matrix of structure vectors}

\item{rational}{should rational arithmetic be used}

\item{model}{model string. One of "bcl", "b", "cl", "acl", "osm", "sl".}

\item{backend}{which backend to use for the linear program. Can be "rcdd" (default and only option for rational=TRUE) or "ROI".}

\item{solver}{the solver to be used in the backend. Defaults to "DualSimplex" for "rcdd" and the first LP solver returned by `ROI_applicable_solver()` for "ROI".}

\item{...}{arguments for the generic: For pre-fit \code{y}, \code{X} with \code{y} a vector of type factor, character, logical, numeric or integer. This is the \code{y} argument of \code{diagsep}. In this case one also needs to supply the argument \code{X} and optional but recommended a \code{model}. One can also supply a matrix \code{S}, in which case we treat it as the \code{S} argument to \code{diagsep}. For post-fit this can currently be an object of class \code{glm}, \code{polr}, \code{clm}, \code{osm} or \code{nnet}.}

\item{formula}{An object of class ‘"formula"’ (or one that can be coerced to that class): a symbolic description of the model to be fitted.  The details of model specification are given under ‘Details’ in \code{\link[stats]{glm}}.}

\item{data}{Either a standard data frame, list or environment (or object coercible by as.data.frame to a data frame) containing variables in the model. If not found in \code{data}, the variables are taken from \code{environment(formula)}, typically the environment from which the function is called. Alternatively, data can be a data frame or matrix containing rational numbers as per the definition in \code{rcdd} (i.e. columns are characters, the entries are either integer numbers or ratios of integer numbers, e.g. "1", or "-234/19008". This is checked internally; see the Details for what happens when this structure is discovered.}

\item{contrasts}{contrasts: an optional list. See the  \code{contrasts.arg} of \code{model.matrix.default}. Only effective for standard data frames.}

\item{object}{model object}
}
\value{
an object of class 'sepmod' that is a list with the components:
\itemize{
\item separation boolean whether there is separation ('TRUE' means separation)
\item septype which type of separation (or not). A string of either "Overlap", "Quasi-Complete Separation" or "Complete Separation".
\item reccdim dimension of recession cone
\item offrows offending rows in X 
\item nr.offcols number of columns of the design matrix that have separation
\item offcols columns of the design matrix that have separation. It is given as category::effect.  
}
}
\description{
This function checks whether there is (quasi-) complete separation, which type if any, gives the dimension of the recession cone, lists the number of columns in the design matrix that give rise to the separation as well as the columns names and lists the rows in X/S for which we have separation.
}
\details{
The function uses either a response vector y and a design matrix X, or a structure vector matrix S. If S is given, y and X and model are ignored.

\code{diagnose_separation} is S3 generic. For developers: If a method should be provided for the generic, it is best to have that method create a matrix of structure vectors \code{S} and use the low-level function \code{diagsep} with it.

The `formula` method is for standard data frames and formulas that work the same way as when used with \code{\link[stats]{glm}}. It does not support extended formulas, and may not work for functions that do formula processing differently. For a data frame/matrix given as rational numbers in the \code{rcdd} definition this is recognized but the formula does not get expanded and is taken literally, so e.g. variables in formula must match exactly with the column names in data, or factors need to be converted to dummies before that (wouldn't be possible in the rational format in any other way anyway).
}
\examples{
data(qcsepdatm)
y<-qcsepdatm$y
X<-cbind(1,qcsepdatm[,2:ncol(qcsepdatm)])
diagsep(y,X,model="bcl")

data(qcsepdatm)

#pre fit
y<-as.factor(qcsepdatm$y)
X<-cbind("(Intercept)"=1,qcsepdatm[,2:ncol(qcsepdatm)])
diagnose_separation(y, X=X, model="bcl") 

#post fit
if (require('nnet')) {
m1 <- nnet::multinom(y~x1+x2,data=qcsepdatm)
diagnose_separation(m1)
}
}
