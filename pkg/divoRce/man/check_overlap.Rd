% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R
\name{check_overlap}
\alias{check_overlap}
\title{Check overlap}
\usage{
check_overlap(..., rational, backend, solver, quick)
}
\arguments{
\item{...}{arguments for the generic: For pre-fit \code{y}, \code{X} with \code{y} a vector of type factor, character, logical, numeric or integer. This is the \code{y} argument of \code{checksep_worker}. In this case one also needs to supply the argument \code{X} and optional but recommended a \code{model}. One can also supply a matrix \code{S}, in which case we treat it as the \code{S} argument to \code{checksep_worker}. For post-fit this can currently be an object of class \code{glm}, \code{polr}, \code{clm}, \code{osm} or \code{nnet}.}

\item{rational}{should rational arithmetic be used}

\item{backend}{which backend to use for the linear program. Can be "rcdd" (default and only option for rational=TRUE) or "ROI".}

\item{solver}{the solver to be used in the backend. Defaults to "DualSimplex" for "rcdd" and the first LP solver returned by `ROI_applicable_solver()` for "ROI".}

\item{quick}{boolean flag whether the quick linear program is to be used or the full fledged one.}
}
\description{
Check overlap
}
\details{
\code{check_overlap} is an S3 generic function. For developers: If a method should be provided for the generic, it is best to have that method create a matrix of structure vectors \code{S} and use the low-level function \code{check_overlap_worker} with it.
}
\examples{

 # pre fit

## Binary data
data(csepdat1)
outc<-csepdat1$y
desma<-cbind("(Intercept)"=1,csepdat1[,2:ncol(csepdat1)])
check_overlap(y = outc, X = desma, model = "b")

# Nominal data 
# Baseline-category link
data(qcsepdatm)
y<-qcsepdatm$y
X<-cbind(1,qcsepdatm[,2:ncol(qcsepdatm)])
check_overlap(y = y, X = X, model = "bcl")

# Ordinal data
data(qcsepdato)
yo<-qcsepdato$y
Xo<-qcsepdato[,2:ncol(qcsepdato)]

# Sequential link
check_overlap(yo, Xo, model = "sl")

# Ordered stereotype
check_overlap(yo, Xo, model = "os")

# Adjacent-category link
check_overlap(yo, Xo, model= "acl")

# Cumulative link
check_overlap(yo, Xo, model = "cl")


# post fit
m1 <- stats::glm(y~x1+x2,data=csepdat1,family=binomial())
check_overlap(m1)
}
