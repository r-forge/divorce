% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checksep.R, R/generics.R
\name{checksep}
\alias{checksep}
\alias{check_separation}
\title{General separation check.}
\usage{
checksep(
  y,
  X,
  S,
  rational = FALSE,
  model = c("b", "bcl", "cl", "acl", "sl", "osm")
)

check_separation(x, rational, ...)
}
\arguments{
\item{y}{a categorical outcome vector}

\item{X}{a design matrix, e.g. generated via a call to 'model.matrix'. This means we expect that X already contains the desired contrasts for factors (e.g., dummies) and any other expanded columns (e.g., for polynominals).}

\item{S}{a matrix of structure vectors}

\item{rational}{should rational arithemtic be used}

\item{model}{what model class is intended to be fitted? Can be any of "b" for binary, "bcl" for baseline-category link, "cl" for cumulative link, "acl" for adjacent-category link. "sl" for sequential link, "osm" for ordered stereotype model. If missing or NULL it defaults to cumulative link for ordinal y and baseline-category for everything else.}

\item{x}{an object. For pre-fit this can be a vector of type factor, character, logical, numeric or integer. This is the y argument of checksep. In this case we also need an X. It can alos be a matrix, in which case we treat it as the S argument to checksep. For post-fit this can currently be an object of class glm, polr, clm, osm or nnet.}

\item{...}{other arguments}
}
\value{
a Boolean; either 'TRUE' if we detected separation or 'FALSE' if not.
}
\description{
This function checks for (quasi-) complete separation by calling the appropriate low-level functions.
}
\details{
The function uses either a response vector y and a design matrix X, or a structure vector matrix S. If S is given, y and X and model are ignored.
}
