% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/methods.R, R/seprows.R
\name{separation_rows}
\alias{separation_rows}
\alias{separation_rows.default}
\alias{separation_rows.factor}
\alias{separation_rows.character}
\alias{separation_rows.logical}
\alias{separation_rows.numeric}
\alias{separation_rows.integer}
\alias{separation_rows.matrix}
\alias{separation_rows.formula}
\alias{separation_rows.osm}
\alias{separation_rows.clm}
\alias{separation_rows.polr}
\alias{separation_rows.multinom}
\alias{separation_rows.glm}
\alias{separation_rows.bracl}
\alias{separation_rows.brmultinom}
\alias{seprows_worker}
\title{Identify the rows in the design/structure vector matrix that cause separation.  It calls lower level functions if given an argument or chooses based on the response type.}
\usage{
separation_rows(..., rational)

\method{separation_rows}{default}(object, rational = FALSE, ...)

\method{separation_rows}{factor}(y, X, rational = FALSE, ...)

\method{separation_rows}{character}(y, X, rational = FALSE, ...)

\method{separation_rows}{logical}(y, X, rational = FALSE, ...)

\method{separation_rows}{numeric}(y, X, rational = FALSE, ...)

\method{separation_rows}{integer}(y, X, rational = FALSE, ...)

\method{separation_rows}{matrix}(S, rational = FALSE, ...)

\method{separation_rows}{formula}(
  formula,
  data,
  model = c("bcl", "b", "cl", "acl", "os", "sl"),
  rational = FALSE,
  contrasts = NULL,
  ...
)

\method{separation_rows}{osm}(object, rational = FALSE, ...)

\method{separation_rows}{clm}(object, rational = FALSE, ...)

\method{separation_rows}{polr}(object, rational = FALSE, ...)

\method{separation_rows}{multinom}(object, rational = FALSE, ...)

\method{separation_rows}{glm}(object, rational = FALSE, ...)

\method{separation_rows}{bracl}(object, rational = FALSE, ...)

\method{separation_rows}{brmultinom}(object, rational = FALSE, ...)

seprows_worker(
  y,
  X,
  S,
  rational = FALSE,
  model = c("bcl", "b", "cl", "acl", "sl", "os")
)
}
\arguments{
\item{...}{arguments for the generic: For pre-fit \code{y}, \code{X} with \code{y} a vector of type factor, character, logical, numeric or integer. This is the \code{y} argument of \code{seprows_worker}. In this case one also needs to supply the argument \code{X} and optional but recommended a \code{model}. One can also supply a matrix \code{S}, in which case we treat it as the \code{S} argument to \code{seprows}. For post-fit this can currently be an object of class \code{glm}, \code{polr}, \code{clm}, \code{os} or \code{nnet}.}

\item{rational}{should rational arithmetic be used?}

\item{object}{model object}

\item{y}{the categorical outcome variable. Can be binary, categorial or ordinal. Works best if it is an ordered or unordered factor but can also be numeric, boolean or character. If y is not a factor, it is treated as a nominal (categorical) outcome.}

\item{X}{a design matrix, e.g. generated via a call to 'model.matrix'. This means we expect that X already contains the desired contrasts for factors (e.g., dummies) and any other expanded columns (e.g., for polynomials).}

\item{S}{a matrix of structure vectors. If given, \code{y} and \code{X} and \code{model} are ignored.}

\item{formula}{An object of class ‘"formula"’ (or one that can be coerced to that class): a symbolic description of the model to be fitted.  The details of model specification are given under ‘Details’ in \code{\link[stats]{glm}}.}

\item{data}{Either a standard data frame, list or environment (or object coercible by as.data.frame to a data frame) containing variables in the model. If not found in \code{data}, the variables are taken from \code{environment(formula)}, typically the environment from which the function is called. Alternatively, data can be a data frame or matrix containing rational numbers as per the definition in \code{rcdd} (i.e. columns are characters, the entries are either integer numbers or ratios of integer numbers, e.g. "1", or "-234/19008". This is checked internally; see the Details for what happens when this structure is discovered.}

\item{model}{what model class is intended to be fitted? Can be any of "b" for binary, "bcl" for baseline-category link, "cl" for cumulative link, "acl" for adjacent-category link. "sl" for sequential link, "os" for ordered stereotype model. If missing it defaults to cumulative link for ordinal y and baseline-category for everything else.}

\item{contrasts}{contrasts: an optional list. See the  \code{contrasts.arg} of \code{model.matrix.default}. Only effective for standard data frames.}
}
\description{
This function checks which rows in X/S are responsible for separation on any category. The observations need not separate the same categories.
}
\details{
\code{separation_rows} is S3 generic. For developers: If a method should be provided for the generic, it is best to have that method create a matrix of structure vectors \code{S} and use the low-level function \code{seprows_worker} with it.

The `formula` method is for standard data frames and formulas that work the same way as when used with \code{\link[stats]{glm}}. It does not support extended formulas, and may not work for functions that do formula processing differently. For a data frame/matrix given as rational numbers in the \code{rcdd} definition this is recognized but the formula does not get expanded and is taken literally, so e.g. variables in formula must match exactly with the column names in data, or factors need to be converted to dummies before that (wouldn't be possible in the rational format in any other way anyway).
}
\examples{
## Binary data
data(csepdat1)
outc<-csepdat1$y
desma<-cbind("(Intercept)"=1,csepdat1[,2:ncol(csepdat1)])
separation_rows(y = outc, X = desma, model = "b")

# Nominal data 
# Baseline-category link
data(qcsepdatm)
y<-qcsepdatm$y
X<-cbind(1,qcsepdatm[,2:ncol(qcsepdatm)])
separation_rows(y = y, X = X, model = "bcl")

# Ordinal data
data(qcsepdato)
yo<-qcsepdato$y
Xo<-qcsepdato[,2:ncol(qcsepdato)]

# Sequential link
separation_rows(yo, Xo, model = "sl")

# Ordered stereotype
separation_rows(yo, Xo, model = "os")

# Adjacent-category link
separation_rows(yo, Xo, model= "acl")

# Cumulative link
separation_rows(yo, Xo, model = "cl")

# post fit
if (require('MASS')) {
m1 <- MASS::polr(y~x1+x2, data = qcsepdato)
separation_rows(m1)
}
}
